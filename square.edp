// Parameters
real L = 1.0; // Dimensão do lado da cavidade unitária

// Building Square
mesh Th = square(10, 10, [x*L, y*L]); 

// Plot Square
plot(Th, wait=1, fill=1);

// Boundary conditions
fespace Vh(Th, [P1, P1]);
Vh [vx, vy], [vx_teste, vy_teste];
